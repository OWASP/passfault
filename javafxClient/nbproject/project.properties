applet.draggable=false
applet.height=900
applet.width=800
application.title=passfault
application.vendor=Cameron Morris
build.classes.excludes=**/*.java,**/*.form,**/*.fx
build.classpath=\
    ${javac.classpath}:\
    ${src.dir}:\
    ${build.classes.dir}:
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
debug.classpath=\
    ${run.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/passfaultFX.jar
dist.javadoc.dir=${dist.dir}/javadoc
excludes=
execution.target=jnlp
includes=**
jar.compress=true
javac.classpath=\
    ${reference.passfault.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=false
javac.source=1.5
javac.target=1.5
javadoc.additionalparam=
javadoc.author=true
javadoc.encoding=
javadoc.private=true
javadoc.version=true
javafx.profile=desktop
jnlp.codebase.type=local
jnlp.codebase.url=file:/C:/Users/cam/workspace/passfaultFX/dist/
jnlp.offline-allowed=true
jnlp.packEnabled=true
jnlp.signed=false
jnlp.update.model=always
main.class=passfault.gui.view.Analysis
pack200.jar.compress=true
platform.active=JavaFX_SDK1.3
project.passfault=../passfault
reference.passfault.jar=${project.passfault}/dist/passfault.jar
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
# Space-separated list of JVM arguments used when running the project
# (you may also define separate properties like run-sys-prop.name=value instead of -Dname=value
# or test-sys-prop.name=value to set system properties for unit tests):
run.jvmargs=
source.encoding=UTF-8
src.dir=src
